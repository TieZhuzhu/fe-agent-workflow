---
description: uni-app 移动端通用代码规范。支持 Vue 2 / Vue 3，按项目实际版本选用，禁止混用。仅适用于 uni-app 项目。
alwaysApply: true
---

# 前端通用代码规范（uni-app 移动端）

## 适用范围

**本规范仅用于 uni-app 移动端项目**（H5 / 微信小程序 / App 等），不适用于 PC 中后台。

## 技术栈与版本判定

写码前读取 `package.json`、`manifest.json` 判定：

| 项 | Vue 3 项目 | Vue 2 项目 |
|----|-----------|-----------|
| 写法 | `<script setup lang="ts">` + Composition API | Options API（`export default { data, methods }`） |
| 状态 | Pinia（优先）或 Vuex | Vuex |
| 接口文件 | `services.ts` | `services.js` |
| 类型 | `types.ts` + JSDoc | `types.js` 或 JSDoc |

**同一项目、同一文件内禁止混用 Vue 2 / Vue 3 写法。**

未检测到 Vue 2 时按 Vue 3 生成；检测到 `vue@^2.x` 时全程走 Vue 2 路径。

## UI 组件优先级（强制）

1. **uni-app 原生组件优先**：`view`、`text`、`image`、`scroll-view`、`swiper`、`button`、`input`、`textarea`、`picker` 等
2. **uview-ui 补充**：原生无法实现或实现成本过高时使用（如 `u-popup`、`u-picker`、`u-tabs`）
3. **uni_modules 官方/三方组件**：项目已接入且团队约定使用时
4. **禁止**引入 Element UI / Element Plus / Ant Design 等 PC 组件库

## 页面与路由

- 路由由 **`pages.json`** 声明，不使用 `vue-router`
- **主包 `pages`**：仅 tabBar 页面 + 启动必要页（登录、引导等）
- **非 tabBar 页面**：必须放 **`subPackages`**，禁止堆入主包（控制主包体积）
- 分包按**功能模块**划分：`product`、`order`、`user`、`shop`、`coupon` 等
- 优先放入**已有子包**；子包页面过多再考虑拆分，**禁止**为单页随意新建子包

详见 **`路由与分包规范.mdc`**、**`项目结构与命名规范.mdc`**。

## 接口组织

| 模式 | 说明 |
|------|------|
| **默认（推荐）** | 页面目录 `services.js` / `services.ts` |
| **存量集中式** | 项目已有 `service/` 或 `services/` 集中目录时，**沿用项目惯例**，不强行拆分 |
| 组件独有 | `components/<Name>/services.*` |
| 全局共用 | 根目录 `service/`、`common/` 或 bootstrap 扫描到的路径 |

以 **`project-conventions.md`**（bootstrap 扫描）与同模块已有页面为准。

## 复用逻辑

| Vue 3 | Vue 2 |
|-------|-------|
| 页面内 `hooks/useXxx.ts` | `mixins/` 或页面 `methods` |
| 跨模块 `@/hooks/` | 跨模块 `common/`、`utils/` |

## 命名

- 文件名、变量名、方法名**统一英文**，禁止拼音
- 方法前缀：`handle`（交互）、`load`（请求）、`on`（Vue 2 生命周期内回调）
- 页面目录 kebab-case；组件目录 PascalCase

## 代码质量

- 精简逻辑，删除冗余代码与 `console.log`
- **删除未使用引入**；交付前 ESLint 零 error（见 `lint-check` Skill）
- **`constants` 与 `utils` 分离**（见项目结构规范）
- 形参超过 2 个用对象传参
- **严禁 mock**，除非用户明确要求

## 接口对接

入参原样透传、出参原样绑定。详见 **`接口对接规范.mdc`**。

## 路径别名

使用项目配置的 `@` 等 alias，**禁止**深层相对路径：

```js
// ✅
import { GetProductList } from './services'
import ProductCard from '@/components/ProductCard'
import request from '@/common/request'

// ❌
import Foo from '../../../components/Foo'
```

## 条件编译

多端差异使用 `#ifdef` / `#ifndef`，禁止运行时 `if (platform)` 替代可静态裁剪的逻辑：

```vue
<!-- #ifdef MP-WEIXIN -->
<view>仅微信小程序</view>
<!-- #endif -->
```

## Skill 路由

| 用户意图 | Skill |
|----------|-------|
| 新建页面 / PRD / Figma / 原型 | `feature-spec` → `feature-analyze` → `feature-dev-workflow` |
| 已有页加功能 | `incremental-feature` |
| 联调 / OpenAPI | `openapi-api-integration` / `api-integration` |
| 注册 pages.json | `route-permission` |
| bug / 报错 | `bugfix-workflow` |
| 规范优化（**单页/单模块**） | `code-normalize` |
| **全量重构** | 先 `【spec】propose project-refactor` → `rules-refactor` → `【verify】project-refactor` |
| **Vue2 升 Vue3** | 先 `【spec】propose vue3-migration` → `vue2-to-vue3-refactor` → `【verify】vue3-migration` |
| spec / analyze / verify / finish / archive | `feature-spec` / `feature-analyze` / `feature-verify` / `feature-finish` / `feature-archive` |
| lint | `lint-check` |
| **新建 / 修改 Skill**、skill-creator、优化 skill description | `skill-creator` |
| 首次接入 | `project-bootstrap` |

完整索引见 `.cursor/skills/README.md`。

## 规范预加载

写码前须 Read `.cursor/skills/shared/rules-activation.md` 并按页面类型加载 rules。**未预加载不得写码。**

## 配置约定

| 项 | Vue 3 | Vue 2 |
|----|-------|-------|
| request | 以 conventions / 项目 services 为准 | 同左 |
| services 导出 | `export const GetXxx = (data) => ...` 箭头函数 | 同左 |
| 页面入口 | `index.vue` 或模块内单文件 `.vue` | 同左 |
| 导航 | `uni.navigateTo` / `uni.switchTab` | 同左 |
