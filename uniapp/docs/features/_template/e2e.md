# E2E 冒烟：<功能名>

> P0 浏览器验收场景。供 `feature-e2e-verify` 或人工冒烟。  
> **勿写入** 真实密码、token；测试账号用「向负责人索取」描述。

## 前置条件

- 环境：`local` / `test` / `staging`
- 入口 URL：`/module/page`（与 `design.md` path 一致）
- 登录：<!-- 测试账号方式，不含 secret -->
- 测试数据：<!-- 如「存在至少 1 条订单」 -->
- **浏览器自动化**（`feature-e2e-verify`）：优先 Browser MCP（**Cursor MCP 进程的 Node ≥18**，须用户重启 MCP/Cursor）；Playwright 降级时 Agent Shell 内 `nvm use 20`

## P0 场景

### P0-1：<主流程名称>

| 步序 | 操作 | 期望 |
|------|------|------|
| 1 | 打开 `<path>` | 页面加载，无白屏 |
| 2 | <!-- When --> | <!-- Then --> |
| 3 | | |

**选择器（可选，便于自动化）：**

- `data-testid="..."` 或 按钮文案「查询」

### P0-2：空态 / 无数据（若适用）

| 步序 | 操作 | 期望 |
|------|------|------|
| 1 | 筛选无结果条件 | 空态展示，不报错 |

### P0-3：错误 / 权限（若适用）

| 步序 | 操作 | 期望 |
|------|------|------|
| 1 | | |

## 不在范围（Out of E2E）

- <!-- 导出大批量、支付、删库等 -->

## 记录

| 日期 | 执行人 | Browser/Playwright/人工 | Node | 结果 |
|------|--------|----------|------|
| | | | |
