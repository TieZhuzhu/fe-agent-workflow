# Field Map: project-refactor

**N/A — 本 feature 为代码规范化重构，不新增接口、不修改字段映射。**

重构遵守 `接口对接规范.mdc`：入参原样透传、出参原样绑定。

## 分页字段参考（仅记录现状，不改语义）

| 页面 | pageNo/page | pageSize | 列表字段 | 总数字段 |
|------|-------------|----------|----------|----------|
| 默认 | `pageNo` | `pageSize` | 以接口文档为准 | |
